{
  "name": "{PACKAGE_NAME}",
  "version": "0.1.0",
  "description": "{DESCRIPTION}",
  "author": "whatwedo <welove@whatwedo.ch>",
  "devDependencies": {
    "standard": "^5.4"
  },
  "scripts": {
    "craffft": "$(npm bin)/craffft",
    "craffft:log": "$(npm bin)/craffft --outputLog",
    "craffft:build": "$(npm bin)/craffft --build",
    "craffft:compile": "$(npm bin)/craffft --build && $(npm bin)/craffft",
    "craffft:compile:log": "$(npm bin)/craffft --outputLog --build && $(npm bin)/craffft --outputLog"
  }
}
